on: workflow_dispatch
jobs:
  http2-main:
    runs-on: ubuntu-26.04-arm
    steps:
      - run: |
          sudo apt-get install -y nghttp2-client
          h2load -c 10 -t $(nproc) -m 100000 -D 10 https://1.1.1.1/
