on: workflow_dispatch
jobs:
  http2-main:
    runs-on: ubuntu-26.04-arm
    steps:
      - run: |
          sudo apt-get install -y nghttp2-client
          h2load -c 100 -t $(nproc) -m 10000 -D 60 https://sub.spacevpn.fun/sub/yyPt4WFDGt4NhBMG
